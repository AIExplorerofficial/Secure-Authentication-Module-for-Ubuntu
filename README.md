# Secure-Authentication-Module-for-Ubuntu
Linux PAM module that adds TOTP-based two-factor authentication (2FA) to SSH, sudo, and console login on Ubuntu — compatible with Google Authenticator, Authy, and any RFC 6238 app. No network calls, no third-party servers.
